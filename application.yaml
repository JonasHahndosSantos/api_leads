openapi:
  title: api_leads API
  version: 1.0.0
  description: API gerada automaticamente pelo Vaden.
  enable: true

postgres:
  host: localhost
  database: banco_portal
  port: 5432
  username: postgres
  password: db
  ssl: disable
server:
  port: 8080
  host: 0.0.0.0

env:
  base_url: http://localhost
  mode: debug